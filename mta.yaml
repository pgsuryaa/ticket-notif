_schema-version: "3.2"
ID: leo
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: leo-destination-content
  type: com.sap.application.content
  requires:
  - name: leo-destination-service
    parameters:
      content-target: true
  - name: leo-repo-host
    parameters:
      service-key:
        name: leo-repo-host-key
  - name: leo-uaa
    parameters:
      service-key:
        name: leo-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: leo_html_repo_host
          ServiceInstanceName: leo-html5-srv
          ServiceKeyName: leo-repo-host-key
          sap.cloud.service: leo
        - Authentication: OAuth2UserTokenExchange
          Name: leo_uaa
          ServiceInstanceName: leo-xsuaa-srv
          ServiceKeyName: leo-uaa-key
          sap.cloud.service: leo
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: leo-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: leo-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - leo.zip
      name: leo
      target-path: resources/
- name: leo
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: leo-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: leo-destination-service
    service-plan: lite
- name: leo-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: leo-xsuaa-srv
    service-plan: application
- name: leo-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: leo-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
